= simple_form_for([:contributors, @application, @article], :defaults => {:input_html => {:class => 'span12', :rows => 3}}) do |f|
  = f.error_notification

  .form-inputs
    = f.association :categories, :collection => @application.categories.all
    = f.input :name
    = f.input :body , :input_html => { :rows => 15}
    = f.input :status, :inline_label => 'Yayınla' , :input_html => {:class => ''}

  .form-actions
    = f.button :submit
