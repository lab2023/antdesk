= simple_form_for([:contributors, @application, @article]) do |f|
  = f.error_notification

  .form-inputs
    = f.association :categories, :collection => @application.categories.all
    = f.input :name
    = f.input :body

  .form-actions
    = f.button :submit
