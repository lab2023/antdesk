= simple_form_for([:contributors, @application, @video]) do |f|
  = f.error_notification

  .form-inputs
    = f.association :categories, :collection => @application.categories.all
    = f.input :name
    = f.input :provider
    = f.input :status, :inline_label => 'Yayınla'


  .form-actions
    = f.button :submit
