= title t('tt.administrator', resource_name: Application.model_name.human)

- content_for :toolbar_left do
  = link_to(contributors_application_path(@application) , class: 'btn', rel: 'tooltip', title: t('tooltips.back')) do
    %i.icon-arrow-left

= form_tag(administrator_contributors_application_path(@application), :method => :post ) do

  %label.string.control-label{:for => "email"} Eposta
  .form-inputs
    = email_field_tag(:email)

  .form-actions
    %input.btn{:type => "submit", :value => "Gönder"}
