%li
  = link_to(category_path(category)) do
    %i{:class => "#{category.icon_name}"}
    = category.name
    %span{:style => "font-weight:normal;"}
      = "(#{category.articles.active.count + category.videos.active.count})"

