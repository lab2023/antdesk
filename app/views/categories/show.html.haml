= title t(@category.name.camelize)
%ul.breadcrumb
  = '<li>'.html_safe
  = render_breadcrumbs :separator => '<span class="divider">/</span></li><li>'.html_safe
  = '</li>'.html_safe


.row-fluid
  .span3
    %ul.nav.nav-tabs.nav-stacked
      = render @current_application.categories
      %p
      = render :partial => 'partials/social'
  .span9
    %h1 Kategoriye Ait Makaleler
    .row-fluid
    - @current_application.categories.find(@category).articles.where('articles.status == ?', true).uniq.each do |article|
      %p
        %h4
          = link_to(article_path(article)) do
            = article.name.camelize
    %h1 Kategoriye Ait Videolar
    .row-fluid
    - @current_application.categories.find(@category).videos.where('videos.status == ?', true).uniq.each do |video|
      %p
        %h4
          = link_to(video_path(video)) do
            = video.name.camelize
